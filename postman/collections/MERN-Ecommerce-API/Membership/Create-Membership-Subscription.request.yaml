$kind: 'http-request'
name: Create Membership Subscription
order: 3000
method: POST
url: '{{baseUrl}}/membership'
body:
  type: json
  content: |-
    {
      "planId": "plan_monthly",
      "returnUrl": "https://yourapp.com/membership/return"
    }
