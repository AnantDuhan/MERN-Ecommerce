$kind: 'http-request'
name: Reset Password
order: 8000
method: PUT
url: '{{baseUrl}}/password/reset/:token'
pathVariables:
  - key: token
    value: '{{resetToken}}'
body:
  type: json
  content: |-
    {
      "password": "newpassword123",
      "confirmPassword": "newpassword123"
    }
